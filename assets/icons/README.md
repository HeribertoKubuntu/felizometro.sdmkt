# Iconos por plataforma

Coloca aqui los iconos finales de build:

- `app.ico` para Windows
- `app.icns` para macOS

Mientras no existan estos archivos, el empaquetador usa el valor de `icon` en `config/apps.json`.
